{
  "name": "pansis-paws-functions",
  "description": "Sends Pansi's Paws invoices when a client's week finishes.",
  "type": "module",
  "main": "index.js",
  "engines": { "node": "20" },
  "scripts": {
    "sync-core": "cp ../invoice-core.js ./invoice-core.js",
    "deploy": "npm run sync-core && firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.1.0"
  },
  "private": true
}
